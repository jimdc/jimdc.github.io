{
  "name": "website",
  "description": "Kabir's website",
  "version": "1.0.0",
  "author": "Kabir Goel",
  "dependencies": {
    "gatsby": "^1.9.42",
    "gatsby-link": "^1.6.15",
    "gatsby-plugin-google-analytics": "^1.0.7",
    "gatsby-plugin-react-helmet": "^1.0.5",
    "gatsby-plugin-typography": "^1.7.9"
  },
  "keywords": [
    "gatsby"
  ],
  "license": "MIT",
  "main": "n/a",
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --trailing-comma es5 --no-semi --single-quote --write 'src/**/*.js' './*.js'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gatsby build && gh-pages --dist public --branch master"
  },
  "devDependencies": {
    "gh-pages": "^1.0.0",
    "prettier": "^1.6.1"
  }
}
